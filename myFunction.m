function [hypotnuse] = myFunction(adj,opp)
%MYFUNCTION calculates hypotenuse from adjacent and opposite sides
%   Use Pythagoras theorm to calculate hypotnuse using input and output
hypotnuse = sqrt((adj^2) + (opp^2));
end

